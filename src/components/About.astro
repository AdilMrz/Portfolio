---
import SectionTitle from './SectionTitle.astro';
---

<section id="about-me" class="about-section">
  <div class="container">
    <SectionTitle title="à propos de moi" />
    <div class="about-content">
      <div class="about-text">
        <p>Salut! je suis Adil</p>
        <br>
        <p>
          Je suis un développeur front-end et Qa testeur junior autodidacte basé à Meknes, Maroc.
          Je peux développer des sites web responsives de zéro et les améliorer
          en expériences utilisateur modernes.
        </p>
        <br>
        <p>
          Transformer ma créativité et mon savoir-faire en sites web a été
          mon passe-temps depuis plus d'un an. J'ai aidé divers clients à
          établir leur présence en ligne. Je m'efforce toujours d'apprendre
          les technologies et les frameworks les plus récents.
        </p>
        <a href="#" class="btn read-more">En savoir plus -&gt;</a>
      </div>
      <div class="about-image">
        <div class="image-wrapper">
          <div class="image-placeholder">
            <img src="/projects/hero.png" alt="Adil Marzouki" style="width: 100%; height: 100%; object-fit: cover;" />
          </div>
          <div class="dots-decoration">
            {Array(25).fill(0).map(() => <div class="dot"></div>)}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    padding: var(--section-padding);
  }

  .container {
    max-width: var(--container-width);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .about-text p {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.7;
  }

  .read-more {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border: 1px solid var(--accent);
    color: var(--text-primary);
    font-size: 1rem;
    transition: all 0.3s ease;
    background: transparent;
    margin-top: 1.5rem;
  }

  .read-more:hover {
    background-color: var(--accent);
    color: var(--bg-primary);
  }

  .about-image {
    display: flex;
    justify-content: flex-end;
  }

  .image-wrapper {
    position: relative;
  }

  .image-placeholder {
    width: 340px;
    height: 400px;
    border: 1px solid var(--accent);
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
  }

  .image-placeholder svg {
    width: 100%;
    height: 100%;
  }

  .dots-decoration {
    position: absolute;
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 4px;
    right: -30px;
    top: 50%;
    transform: translateY(-50%);
  }

  .dot {
    width: 4px;
    height: 4px;
    background-color: var(--text-secondary);
    border-radius: 50%;
  }

  @media (max-width: 768px) {
    .about-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .about-image {
      justify-content: center;
    }

    .image-placeholder {
      width: 280px;
      height: 340px;
    }
  }
</style>
